spring:
  datasource:
    url: jdbc:h2:mem:meanmachinesh2
    username: sa
    password: password
    driverClassName: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none

storage:
  media-dir: C:\Users\Tony\dev\MeanMachines\MeanStreamMachine\upload_tmp
logging:
  level:
    root: info

exceptions:
  STACK_VERBOSE: true